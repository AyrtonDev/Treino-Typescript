let age: number = 65

const word: string = 'Hello Word!'

// nosso target, pode ser configurado para o tipo de EcmaScript que sera compilado, porque nem todos os navegadores suportam o ES6, nesse exemplo foi compilado para ES5