const nome: string = 'Ayrton'

const blabla = (n1: number): string =>{
   return `O parˆmetro era: ${n1}`
}

blabla(15)

// para acionar o modo assistido, no final do comando tsc src/scrpit, coloca -w ou --watchMode

